---
name: 구현계획 — docs LLM 토큰 효율 향상 (Phase 1)
purpose: frontmatter+TL;DR+폴더 README 컨벤션과 단계별 적용 계획
type: tasks/구현계획
applies_to: [root]
tags: [meta, docs, llm-efficiency, frontmatter, lazy-loading]
---

# 구현계획: docs/ LLM 토큰 효율 향상 (Phase 1)

**TL;DR**: md 형식 유지하되 사용 패턴을 바꿔 lazy loading 가능하게 — frontmatter+TL;DR로 본문 안 읽고도 필요성 판단, 폴더 README로 폴더 단위 인덱싱. Phase 1만으로 ~30% 토큰 절감 예상. Phase 3 YAML 분리는 YAGNI로 미진행.

## 목표
md 형식은 유지하되 사용 패턴을 바꿔 LLM의 lazy loading을 가능하게 함. Phase 1만 적용해도 ~30% 토큰 절감 예상.

## 도입 패턴
1. **Frontmatter (YAML 메타) + 1줄 TL;DR** — 본문 안 읽고도 파일 필요성 판단 가능
2. **폴더별 README** — 폴더 단위 lazy load (CLAUDE.md 루트 표 패턴을 폴더 단위로 확장)
3. ~~산문 → YAML 분리~~ — Phase 3 (YAGNI, 미진행)

## 컨벤션 (Phase 1.1에서 §9로 정의)

### Frontmatter 필드
- `name` (필수): 파일 제목 (한국어)
- `purpose` (필수): 한 줄 목적 (50자 이내)
- `type` (필수): `지침` / `회고` / `사용방법` / `이력` / `참고` / `tasks` / `메타`
- `applies_to` (선택): `[root]` / `[projects]` / `[Sound1]` 등 — 적용 범위 한정 시
- `tags` (선택): 검색·필터용 키워드 배열

### TL;DR 규칙
- 첫 헤더(`# 제목`) 바로 아래 1-3줄
- 형식: `**TL;DR**: <한 줄 결론>`

### 폴더 README 형식
- 첫 줄: 폴더 목적 한 줄
- 표: 파일 + 1줄 설명 + 사용 시점

## Phase 1 단계

### 1.1 컨벤션 정의 + 자기 적용
- `docs/지침/일반/문서 작성 규칙.md` §9 신규 추가 (위 컨벤션 명문화)
- 자기 자신에 frontmatter+TL;DR 적용
- 커밋 → 사용자 컨벤션 검토

### 1.2 지침 인덱스 신규
- `docs/지침/README.md` 신규 — CLAUDE.md 표 패턴을 지침/ 폴더 단위로

### 1.3 P1 3개 파일 frontmatter+TL;DR
- `docs/지침/작업 규칙.md` (마스터 체크리스트, 69줄)
- `docs/지침/Git/워크플로우.md` (234줄)
- `docs/지침/코딩/분석·디버깅 규칙.md` (163줄)
- (`docs/지침/일반/문서 작성 규칙.md`는 1.1에서 자기 적용)

## 검증
- frontmatter YAML 파싱 가능 (python yaml)
- 모든 P1 파일에 `**TL;DR**:` 존재 (grep)
- 폴더 README가 형제 파일 모두 인덱싱 (수동 비교)
- 기존 링크 무손상 (frontmatter는 헤더 위, 앵커 영향 없음)

## 회귀 위험 및 회피책
| 위험 | 회피책 |
|---|---|
| 회고 시스템 간섭 | 회고 항목 형식(`## 제목 (날짜)` + Why/How)과 frontmatter는 별개 — 충돌 없음 |
| 링크 깨짐 | frontmatter는 첫 헤더 위에 추가, 헤더·앵커 위치 불변 |
| CLAUDE.md 표와 중복 | §9에 역할 분리 명문화 (CLAUDE.md=최상위 진입점, 폴더 README=폴더 상세) |

## 후속
- Phase 2 (선택): 나머지 지침·회고 파일 적용 + `사용방법/`·`tasks/` README
- Phase 3: 미진행 (YAGNI — `tasks/list.md` 표 형식이 사람·LLM 양쪽에 충분히 효율적)

## 참고
- 원 plan 파일: `~/.claude/plans/md-enchanted-wall.md` (사용자 로컬, git 비추적)
- Explore 진단 + 직접 검증 결과는 plan 파일 "진단 요약" 섹션 참조
