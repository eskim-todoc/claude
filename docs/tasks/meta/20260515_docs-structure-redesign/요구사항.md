---
name: 요구사항 — docs 구조 근본 재설계 (Task α 정책)
purpose: archive 폐지·진행상황/이력 통합·list 단일표·영속 콘텐츠 Wiki 통합 정책·지침
type: tasks/요구사항
applies_to: [root, projects/Sound1, projects/auto-rtt-viewer, projects/ez8300-study, projects/sound1-fw-extractor, projects/wiki]
maturity: stable
tags: [meta, docs, policy-redesign, archive, wiki-integration, simplification]
---

# 요구사항: docs 구조 근본 재설계 (Task α — 정책 + 지침)

**TL;DR**: 사용자가 docs 구조의 근본 가정을 재검토. 결정 4가지 — (A) Archive 폐지, (B) 진행상황·이력 통합, (C) 작업 목록.md 단일표 + status 컬럼, (D) 영속 콘텐츠 Wiki 통합. 본 task는 **정책 결정 + 지침 갱신만**(Task α). 기존 25 작업의 구조 마이그레이션(Task β)과 사용방법·참고의 Wiki 이전(Task γ)은 별도 후속 작업. 본 task 자체는 현 구조로 진행하고 종료 시 마지막 archive 사용 사례로 남음 — Task β에서 일괄 변환됨.

## 1. 사용자 요청

사용자 지시 원문:

> "Y. 영속 콘텐츠 → Wiki 통합 뿐만 아니라, 아래와 같이 핵심부터 변경을 고려하고 있어. 루트의 지침부터 말이야.
> 1. 작업 완료 시 아카이브 이동에 대한 필요성
> 2. 각 작업 별로 생성되는 파일의 양. 이력과 진행상황의 차이가 크게 뭐지?
> 3. 위의 변경을 고려해서 리스트나 이력 관리가 어떻게 바뀌면 좋을까?"
>
> 합의: A+B+C+D 전부 적용 / Task α(정책) 먼저 → β(구조)·γ(Wiki) 순차

## 2. 목표 — 4가지 변화 정책

### 요구사항_1 — Archive 폐지 (A)

| 항목 | 정책 |
|---|---|
| 작업 폴더 위치 | 완료 후에도 **`tasks/<모듈>/<작업>/` 그대로**. `_archive/` 폴더 폐지 |
| 활성/완료 분리 | `작업 목록.md` 단일 표의 `상태` 컬럼 + 작업 폴더 frontmatter `status` 필드로 식별 |
| 외부 참조 안정 | 폴더 위치 영구 → Wiki 가공 페이지의 `sources` 링크·회고의 작업 인용·CLAUDE.md 링크 등이 변동 없음 |
| 마이그레이션 대상 | 기존 5 repo의 `_archive/<모듈>/<작업>/` 폴더 전부 → `tasks/<모듈>/<작업>/`로 복귀 (Task β) |
| `_archive/모듈 현황.md` | Task β 시점에 `tasks/_index.md`로 위치·이름 변경 (또는 폐지·재설계 결정 — 분석에서) |

### 요구사항_2 — 진행상황·이력 통합 (B)

| 항목 | 정책 |
|---|---|
| 통합 파일명 | `이력 및 결과.md` 유지 (작업의 전체 history) |
| 폐지 파일 | `진행상황.md` |
| 작성 시점 | 단계 ① 시작과 동시에 `이력 및 결과.md` 생성 → 진행 중 시계열 누적 → 완료 시 final 요약 섹션 추가 |
| 구조 | (1) frontmatter (2) TL;DR (3) **시계열 로그** 표 (구 진행상황 누적) (4) **완료 요약** 섹션 (구 이력 — 완료 시 추가) |
| 작업당 파일 수 | 5 → **4** (요구사항·분석·계획·이력) |

### 요구사항_3 — 작업 목록.md 단일 표 + status 컬럼 (C)

| 항목 | 정책 |
|---|---|
| 표 분리 | **§활성·§완료 분리 폐지** — 단일 `## 작업 목록` 표 1개 |
| 컬럼 | `작업명 \| 모듈 \| 태그 \| 상태 \| 시작·완료 \| 폴더 \| 요약` |
| 상태 값 | `진행`·`대기`·`차단`·`완료`·`취소` (현 정의 그대로) |
| 정렬 | 시작일 내림차순 디폴트. 활성만 보기는 상태 컬럼 grep/필터 |
| 갱신 이력 | 표 아래 `## 갱신 이력` 그대로 유지 (작업 등재·완료 추적) |
| 마이그레이션 | Task β 시점에 기존 25 작업의 활성/완료 표를 단일 표로 일괄 재작성 |

### 요구사항_4 — 영속 콘텐츠 Wiki 통합 (D, 직전 Y 합의)

| 항목 | 정책 |
|---|---|
| 이전 대상 | 각 repo `docs/사용방법/` + `docs/참고/` |
| 이전 위치 | `projects/wiki/사용방법/` + `projects/wiki/참고/` (Wiki Vault 안 카테고리 폴더) |
| 잔존 폴더 | 각 repo `docs/`에는 `tasks/`(작업 산출물) + `지침/`(Claude 메타 — 프로젝트 특화 시) + `회고/`(루트만, 프로젝트는 없음) |
| 프로젝트 표준 폴더 | `프로젝트 초기 세팅.md §2.3`에서 `사용방법/`·`참고/`를 표준에서 **제외**. 신규 프로젝트는 `tasks/`만 필수 |
| Wiki 주제 페이지 frontmatter | 직전 합의 — `sources`(repo·archive_path·last_synced)·`related`(위키링크) |
| 마이그레이션 | Task γ에서 실제 파일 이전 + 위키링크 재배선 |

### 요구사항_5 — 본 Task α 범위 한정

본 task는 **정책 결정 + 지침 갱신만** 수행. 다음은 본 task **밖**:

- 기존 25 작업의 archive → 활성 복귀 (Task β)
- 기존 진행상황 → 이력 통합 변환 (Task β)
- 기존 작업 목록.md 활성/완료 → 단일표 재작성 (Task β)
- `사용방법/`·`참고/` 파일들의 Wiki Vault 이전 (Task γ)

### 요구사항_6 — 지침 갱신 영향 파일

| 경로 | 변경 |
|---|---|
| `docs/지침/작업 규칙.md` | §3 작업 진행 — 4단계 산출물 5 → 4 (진행상황 제거), §3 종료 시 archive 이동 한 줄 제거 (단일표·status로 대체) |
| `docs/지침/일반/작업 진행 규칙.md` | §2 흐름도 (archive 노드 제거), §2.2 산출물(진행상황 제거), §3 진행상황 문서 절 전체 삭제 또는 이력 및 결과.md §시계열 로그로 흡수, §6 완료 처리(archive 단계 제거 + 작업 목록.md 상태 변경으로 대체) |
| `docs/지침/일반/문서 작성 규칙.md` | §3.1 폴더 트리(`_archive/` 제거 또는 폐지 명시), §3.3(진행상황 의무 제거), §3.4 archive 절차 전체 삭제 또는 "작업 목록.md 상태 변경 + 이력 및 결과.md final 섹션 추가"로 대체, §4 작업 목록.md 단일 표 형식 |
| `docs/지침/일반/프로젝트 초기 세팅.md` | §2.3 표준 폴더에서 `사용방법/`·`참고/` 제외, `_archive/` 자동 생성 NOTE 제거 |
| `CLAUDE.md` (루트) | Layout 트리(docs 폴더 구성 갱신), Projects 표 비고 (필요 시) |
| `docs/회고/작업 진행.md` | "정책 신설 자기 참조" 회고에 본 사례 추가 (자기 참조 + 다중 위치 점검 패턴의 또 다른 적용) |

## 3. 수용 기준 (Definition of Done)

### 3.1 기능·산출물

- [x] 검증_1: 4가지 변화 정책(A·B·C·D) 모두 지침에 명문화
- [x] 검증_2: 5개 지침 파일 + CLAUDE.md 일관 갱신 (한 변화에 대한 표현이 흩어진 모든 위치 grep 점검 — 자기 참조 회고 적용)
- [x] 검증_3: Task β·γ를 위한 명확한 후속 분기 (본 task 산출물에 Task β·γ 진입 조건·범위 명시)
- [x] 검증_4: 새 정책으로 신규 작업 시작 가능한 상태 (지침이 모순 없이 새 흐름 안내)
- [x] 검증_5: 기존 25 작업 + archive와 충돌 없음 (Task β 진행 전까지는 기존 구조 그대로 유효)
- [x] 검증_6: 본 task 산출물 자체는 현 구조(요구사항·분석·계획·진행상황·이력)로 작성 — Task β에서 함께 변환

### 3.2 정합성·검증
- [x] git diff로 모든 변경 검토 가능
- [x] cross-link 살아있음 (broken link 0)
- [x] `이력 및 결과.md` 시계열 로그 + 완료 요약 작성

### 3.3 회귀 방지
- [x] 본 작업은 `claude_feature_docs-structure-redesign` 브랜치 격리
- [x] 각 commit이 atomic — 단계별 revert 가능

## 4. 작업 범위 외 (Out of Scope)

| 항목 | 사유 |
|---|---|
| 기존 25 작업의 구조 마이그레이션 | Task β |
| `사용방법/`·`참고/` 파일들의 실제 Wiki 이전 | Task γ |
| Wiki 주제 페이지 본문 작성 | 사용자 콘텐츠 운영 단계 |
| `tasks/_index.md` 또는 `모듈 현황.md` 후속 형식 결정 | Task β 분석에서 결정 (archive 폐지 후 인덱스 위치·형식 변경 필요) |
| 본 task 자체에 새 정책 자기 적용 | Task β에서 25 작업 일괄 변환 시 본 task도 같이 변환됨. 본 task는 현 구조로 진행 |

## 5. 위임 범위 (Claude 자율 결정)

위임 범위:

- 위임_1: 4 변화 합의 후 25곳 패치 본문 자동 작성
- 위임_2: 본 Task α 자기 적용 면제 결정 (시간 순서 어긋남)
- 위임_3: 응답 분리 다중 Edit 충돌 회피
- 위임_4: 회고 보강 본문

위임 범위 밖:

- 사용자결정_1: 4 변화 채택 합의됨
- 사용자결정_2: Task β·γ 분할 합의됨
- 사용자결정_3: 커밋·병합

## 6. 관련 산출물

- [분석.md](분석.md) — 4가지 변화의 영향 라인 25곳 식별·핵심 결정 4건
- [계획.md](계획.md) — 25곳 패치 본문·다중 Edit 회피 전략·Git 흐름·후속 Task β·γ 진입 조건
- [이력 및 결과.md](이력%20및%20결과.md) — 시계열 로그·완료 요약·구 진행상황 흡수

## 자체 검토

### 5.1 지침 준수

| 지침 | 준수 |
|---|---|
| `작업 진행 규칙.md` 4단계 | 완료 — 본 task는 정책 변경이라 4단계 적용 |
| `명령 해석 규칙.md` | 완료 — 사용자 3 질문에 분석·옵션 제시 → 4 변화 합의 + 작업 분할 합의 |
| `계획 모드 자동 진입.md` | 완료 — Plan Mode 진입 예정 |
| `문서 작성 규칙.md` §9 frontmatter+TL;DR | 완료 |
| `문서 작성 규칙.md` §6.5 한글 라벨 | 완료 — `요구사항_1`~`_6`, `검증_1`~`_6` |
| 회고 "정책 신설 자기 참조 + 다중 위치 grep 점검" | 완료 — 검증_2에 명시 — 정책 흩어진 위치 모두 grep |

### 5.2 논리적 정합성

| 점검 | 결과 |
|---|---|
| 4 변화가 서로 정합 | 완료 — A·B·C는 task 구조 변경, D는 영속 콘텐츠 위치. 독립적이나 한 묶음 정책으로 일관 |
| 본 task가 자기 적용 면제 (Task β에서 일괄)인 사유 | 완료 — 비요구사항에 명시 — 자기 적용은 Task β에서 일괄 변환 |
| Task α → β → γ 의존성 | 완료 — 정책 합의 후 마이그레이션(β)·Wiki 이전(γ) 순서. β·γ는 정책 의존만, 서로 독립 |

### 5.3 미해결 이슈 (분석·계획에서 해소)

| 이슈 | 처리 |
|---|---|
| `tasks/_index.md`(구 `모듈 현황.md`) 형식 — archive 폐지 후 단일 위치로 옮길지·이름 변경할지 | 분석 §3에서 결정 |
| 진행상황 시계열 로그 + 이력 final 요약 통합 시 본문 구조 | 계획 §2 이력 및 결과.md 본문 초안 |
| 기존 25 작업의 진행상황 정보 보존 — Task β에서 어떻게 이력 및 결과.md로 흡수 | Task β 요구사항에서 |
| 작업 목록.md 단일표 정렬 디폴트 (시작일 내림 vs 상태 우선) | 분석에서 |
| 영속 폴더에 frontmatter 점검 — 영속 폴더 정의에서 `사용방법/`·`참고/` 제거 시 다른 곳에 영향 | 분석에서 점검 |
| Wiki Vault 카테고리 폴더 매핑 — `프로젝트/`(직전 합의) vs `사용방법/`·`참고/` 추가 어떻게 양립 | Task γ에서 |
| 본 task가 현 구조로 진행하면서 archive 이동까지 한 후 Task β에서 다시 활성 복귀 — 이중 작업 비효율 vs 안전성 | 분석에서 trade-off |
