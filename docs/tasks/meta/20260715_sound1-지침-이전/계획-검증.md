---
name: 계획 검증 — sound1 지침 2종 루트 이전·범용화
purpose: 계획 산출물 자체 검증 (실패 시나리오·영향 전수·hop 수)
type: tasks/검증
applies_to: [root]
tags: [meta, verify, 계획]
---

# 계획 검증: sound1 지침 2종 루트 이전·범용화

**TL;DR**: **보완 후 통과**. 실패 시나리오 3건 도출 중 **결함 1건 발견** — 훅을 `지침/` 하위에만 걸면 CLAUDE.md 자동주입에 없어 세션이 상기하지 못한다(2026-07-13 오케 로그 누락과 **동일한 실패 패턴**). 계획 Step2에 CLAUDE.md §7 한 줄 훅을 추가해 보완. 경로 참조 전수·hop 수 확인 완료.

## 1. 지침 준수 여부

| 지침 | 준수 |
|---|---|
| [`작업-프로세스.md`](../../../../지침/일반/작업/작업-프로세스.md) | 준수 — ⑤ 계획, ⑥ 검증 독립 파일, 승인 게이트 ⑥ 직후 |
| [`양식/계획.md`](../../../../지침/문서/양식/계획.md) | 준수 — 7섹션 + 자기 적용 검토(학습노트 5.10) |
| [`활용.md §3.1`](../../../../지침/서브에이전트/활용.md)·[`오케스트레이션.md §7`](../../../../지침/서브에이전트/오케스트레이션.md) | 준수 — 확정 스펙 저술은 직접(폭주 방지), fan-out 없어 에이전트-로그 대상 아님 |
| [`Git/브랜치, 병합 규칙.md`](../../../../지침/Git/브랜치,%20병합%20규칙.md) | 준수 — rules feature 브랜치 격리, sound1 경로 명시 add |

## 2. 논리적 정합성

| 점검 | 결과 |
|---|---|
| 분석 결정이 계획에 반영 | 결정_1→Step1 파일명, 결정_2·3→Step1a, 결정_4→Step1b, 결정_5→Step2, 결정_6→Step3 |
| 단계 시퀀스 의존성 명확 | Step1→2(훅이 신규 파일 링크), Step2→3(루트 확립 후 원본 제거) |
| 위험·완화 식별 | main.c 혼입(1순위)·훅 비대·단계 번호·의도 침범·핵심 상실 5건 |
| 서브에이전트 분담 명시 | 직접 처리 + 근거(확정 스펙) + 로그 미대상 사유 |

## 3. 근거 검증 (⑥ 계획 게이트)

### 3.1 실패 시나리오 (≥1 필수)

- **시나리오_1 (범용화 과잉)**: 벤더 어휘를 걷어내다 5게이트가 추상 문구만 남아 실전에서 안 쓰임. → 완화: 범용 MCU 구체 예시(타이머 prescaler)로 판정 흐름 시연 유지(선택지_B 채택 근거와 일치). ⑧에서 원본 대조.
- **시나리오_2 (상기 실패) — 결함 발견**: 훅을 `지침/` 하위에만 걸면 **CLAUDE.md 자동주입 경로에 없어** 세션이 지침 존재 자체를 잊는다. 이는 [20260714 작업](../20260714_지침체계-개편/분석.md)에서 규명한 오케 로그 누락의 root cause(가설_2 "상기 실패")와 **동일 패턴**이다 — 지침에만 있고 자동주입에 없으면 실행되지 않는다. → **계획 보완**: Step2에 `CLAUDE.md §7`(프로그래밍 작업) 한 줄 훅 추가.
- **시나리오_3 (main.c 혼입)**: sound1 커밋에 은수님 코드 작업이 섞임. → 완화: 경로 명시 add + staged 목록 사전 확인(위험표 1순위, Step3 검증에 명시).

### 3.2 경로·파일명 변경 영향 전수 (Grep)

| 변경 | 영향 참조 | 근거 |
|---|---|---|
| sound1 `docs/지침/` 2파일 삭제 | **활성 링크 0건** | ④ 검증 재확인 패스 — repo 전체 Grep 결과 2파일을 가리키는 활성 참조 없음. 내부 `[README.md](README.md)`는 이미 broken(README 기삭제) → 삭제로 해소 |
| 루트 신규 2파일 | 신규 경로 — 기존 참조 없음 | 훅(Step2)에서 새로 링크 |
| 과거 tasks의 옛 `docs/지침/` 참조 | 범위 외 | 소급 수정 금지([`경로·참조 표기.md`](../../../../지침/공통/경로·참조%20표기.md)), 삭제 대상과 무관 |

### 3.3 상대링크 hop 수 (신규 파일 기준)

원본은 sound1 `docs/지침/`에서 루트를 **절대경로**로 참조했으나, 신규 파일은 루트 `지침/코딩/` 내부라 **상대경로**로 바뀐다 — 저술 시 hop 재계산 필요:

| 신규 파일에서 → 대상 | hop | 상대경로 |
|---|---|---|
| `지침/코딩/*.md` → `지침/코딩/*.md` (동일 폴더) | 0 up | `작업 규칙.md` |
| `지침/코딩/*.md` → `지침/일반/작업/작업-프로세스.md` | 1 up | `../일반/작업/작업-프로세스.md` |
| `지침/코딩/*.md` → `지침/문서/양식/계획.md` | 1 up | `../문서/양식/계획.md` |
| `지침/코딩/*.md` → `지침/README.md` | 1 up | `../README.md` |

## 4. 미해결 이슈

| 이슈 | 처리 |
|---|---|
| CLAUDE.md §7 훅 누락 | **계획 Step2에 추가로 보완 완료** (시나리오_2) |
| 작업-프로세스 §1.1 훅 과밀 | 계획 양식으로 갈음 — ⑦ 구현 시 §1.1 조밀도 보고 최종 판단(결정_5) |

## 5. 판정

**보완 후 통과** — 시나리오_2 결함을 계획에 반영(CLAUDE.md §7 훅 추가)한 뒤 사용자 승인 요청 → ⑦ 구현 진입.
