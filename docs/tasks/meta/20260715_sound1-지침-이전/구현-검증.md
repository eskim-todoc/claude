---
name: 구현 검증 — sound1 지침 2종 루트 이전·범용화
purpose: 구현 결과 자체 검증 (수용 기준·링크·원본 대조·main.c 보호)
type: tasks/검증
applies_to: [root, projects/sound1-fw-e8300]
tags: [meta, verify, 구현]
---

# 구현 검증: sound1 지침 2종 루트 이전·범용화

**TL;DR**: 통과. 수용 기준 6항목 충족 — 신규 2종에 벤더 어휘 0건, broken link 0(391링크/45파일), 옛 단계 표기 0, sound1 지침 폴더 소멸 + `main.c` 미접촉 확인. 원본 대조로 5게이트·계층 모델 골격 보존 확인. **이슈 1건**: sound1은 feature 브랜치 없이 `claude_develop` 직접 커밋(사유·판단 §4 기재).

## 1. 지침 준수 여부

| 지침 | 준수 |
|---|---|
| [`작업-프로세스.md`](../../../../지침/일반/작업/작업-프로세스.md) | 준수 — 8단계 ①~⑧ 전 산출물 생성(첫 실적용) |
| [`문서 작성 규칙.md`](../../../../지침/문서/작성%20규칙.md) | 준수 — 신규 2종 frontmatter+TL;DR, GFM·Alert·Mermaid, 한글 라벨(`유닛_1`·`미확인_1`) |
| [`메타-frontmatter.md §4`](../../../../지침/문서/메타-frontmatter.md) | 준수 — 훅은 "한 줄+링크", 본문 SSOT 1곳 (dedup-check) |
| [`활용.md §3.1`](../../../../지침/서브에이전트/활용.md) | 준수 — 확정 스펙 저술이라 fan-out 없이 직접(폭주 방지) |
| [`Git/브랜치, 병합 규칙.md`](../../../../지침/Git/브랜치,%20병합%20규칙.md) | **부분** — rules는 feature 브랜치 격리 준수. sound1은 `claude_develop` 직접 커밋(§4 이슈_1). `main` 직접 커밋 금지·identity·AI 표시 금지는 준수 |

## 2. 논리적 정합성

| 점검 | 결과 |
|---|---|
| 계획 Step1~4 실행 | 전부 완료 (Step2에 ⑥ 검증 보완분 CLAUDE §7 훅 포함) |
| 요구사항 목표 4건 반영 | 목표_1(범용화)·목표_2(승격)·목표_3(4방향 훅)·목표_4(원본 정리) 모두 구현 |
| 원본 골격 보존 | 5게이트(대상 식별·근거·시뮬·부작용·불확실성) 및 계층 3핵심(유닛≠모듈 자동승격·통합테스트 정의·합성 규칙) 원문 의미 유지 — 원본 대조 완료 |
| 훅 상충 없음 | 계층화 §6 "단순·특화" ↔ 구현 패턴 §예외 "과도 추상화 회피" 같은 방향으로 명시 |

## 3. 근거 검증 (⑧ 구현 게이트)

| 게이트 항목 | 근거(명령 출력) | 통과 |
|---|---|---|
| 벤더 어휘 제거 (수용기준_1) | `Grep "IQS323\|Azoteq\|Re-ATI\|Reseed\|LTA\|CFX\|DMIC\|빔포밍"` → 신규 2종 매치 0. `I2S` 1건은 MCU peripheral 예시 나열(범용 명칭, 벤더 무관). 나머지 매치는 기존 파일(네이밍 컨벤션 `tdc_drv_iqs323` 예시·분석디버깅 Sound1 실사례)로 대상 외 | 통과 |
| maturity 잔존 0 | `Grep maturity 지침/코딩` → 0건 (2026-07-14 폐지 정책 준수) | 통과 |
| broken link 0 (수용기준 3.2) | `tools/check-links.ps1` → **391 상대링크·45파일·깨진 링크 0건** (작업 전 349/42 → 신규 2종+훅 반영으로 증가) | 통과 |
| 옛 단계 표기 잔존 0 | `Grep "②분석\|③계획\|단계 ④ 구현"` → 0건. 매치 3건은 `프로젝트 초기 세팅.md`의 **자체 세팅 절차** 단계 ②(CLAUDE.md 작성)로 작업 프로세스와 무관 | 통과 |
| 4방향 훅 도달 (수용기준_3) | CLAUDE.md §7(자동주입 2링크)·구현 패턴 §계층 인식 설계·코딩 작업 규칙 §4.4·§4.5·§6·분석디버깅 §4 NOTE·계획 양식 사용법6·관련지침 | 통과 |
| README 인덱스 (수용기준_4) | `지침/README.md` 코딩 카테고리 2행 추가 | 통과 |
| sound1 폴더 소멸 (수용기준_5) | `ls docs/지침` → "폴더 소멸 확인". 커밋 `b68319b` 4 files(README+2종 삭제, CLAUDE.md 정합) | 통과 |
| **main.c 보호** (수용기준 3.3) | staged 목록에 `main.c` 부재 확인(`grep -q main.c` → "OK: main.c 미포함"). 커밋 후 `git status` → ` M src/2__cm3/Cortex-M3-src/main.c` 잔존 = 은수님 작업 그대로 보존 | 통과 |
| sound1 identity | `git log -1 --format` → `A:김은수 <eunsu.kim@to-doc.com> C:김은수`, AI 표시 없음 | 통과 |

## 4. 미해결 이슈

| 이슈 | 처리 |
|---|---|
| **이슈_1**: sound1은 feature 브랜치 없이 `claude_develop` 직접 커밋 | 은수님이 **같은 브랜치에서 `main.c` 작업 중**이라 feature 분기 시 미커밋 변경이 따라와 오히려 혼선·오염 위험이 커진다고 판단. `main` 직접 커밋 금지는 준수(claude_ 계열 브랜치). **은수님 보고 후 판단 요청** — 필요 시 사후 분기 가능 |
| sound1 push·rules 병합 | 사용자 승인 영역 — 보고 후 진행 |
| 과거 tasks의 옛 `docs/지침/` 경로 참조(sound1) | 범위 외(소급 수정 금지). 필요 시 별도 작업 분기 |

## 5. 판정

**통과** — 수용 기준 전 항목 충족. 잔여는 이슈_1 보고와 병합·push 승인.
