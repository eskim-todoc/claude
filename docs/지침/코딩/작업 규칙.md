---
name: 프로그래밍 작업 규칙
purpose: 코딩 작업의 4단계 프로세스 적용 시 코딩 특화 사항
type: 지침
applies_to: [root, projects]
tags: [coding, workflow, documentation-first]
---

# 프로그래밍 작업 규칙

**TL;DR**: 일반 [`작업 진행 규칙`](../일반/작업%20진행%20규칙.md)의 4단계 프로세스를 코딩 작업에 적용. 코드 작성은 단계 ④. 단계 ③의 산출물 `계획.md`가 곧 구현 계획. 코딩 특화 산출물(시험방법·사용방법)·`tdc_` 접두어 등이 추가로 적용된다.

## 1. 일반 규칙 상속

본 문서는 [`일반/작업 진행 규칙.md`](../일반/작업%20진행%20규칙.md)을 상속한다. 4단계 프로세스·산출물·자체 검토·진행상황·이력의 일반 규칙은 그대로 적용되며, **본 문서는 코딩 작업의 추가·특화 사항만 다룬다.**

> [!IMPORTANT]
> 코드 작성은 단계 ④(구현·처리)에 해당한다. 단계 ①~③의 산출물(`요구사항.md`·`분석.md`·`계획.md`)을 작성하고 사용자 승인을 받기 전에는 **코드를 작성하지 않는다.**

## 2. 코딩 작업의 4단계 매핑

| 일반 단계 | 코딩 작업 시 |
|---|---|
| ① 요구사항.md | 무엇을 구현할지·기능 범위·인터페이스·검증 기준 |
| ② 분석.md | 현행 코드 구조·호출 경로·영향 모듈·회귀 위험 |
| ③ 계획.md | 변경 파일·함수·새 심볼·구현 순서 (기존 `구현계획.md`도 호환) |
| ④ 구현 | 코드 작성·빌드·시험. 필요 시 `시험방법/`·`사용방법/` 추가 산출 |

## 3. 코딩 특화 산출물

| 문서 | 조건 | 배치 |
|---|---|---|
| `시험방법/<...>.md` | 시험 절차·기준 정의 (영속) | `docs/시험방법/` |
| `사용방법/<...>.md` | 사용자 명령 입력·CLI 등 사용법 안내 (영속) | `docs/사용방법/` |

> [!NOTE]
> 작업 범위가 큰 기능이거나 외부에서 재현할 수 있도록 절차가 필요한 경우, 단계 ④ 구현 도중 또는 직후에 작성한다.

## 4. 코딩 특화 규약

### 4.1 신규 심볼·모듈은 `tdc_` 접두어

- 신규 심볼·모듈 분리 시 **`tdc_` 접두어 무조건 적용**
- 기존 파일 리팩토링 시 손댄 심볼은 적용 우선
- 상세: [`네이밍 컨벤션.md`](네이밍%20컨벤션.md)

### 4.2 분석·디버깅

- 회귀 fix·버그 분석 시 [`분석·디버깅 규칙.md`](분석·디버깅%20규칙.md) 추가 적용
  - 호출 경로 grep 검증
  - root cause 가설 최소 3개 cross-check
  - 사용자 변경 단서(system-reminder) 의도 분석

### 4.3 구현 패턴

- FSM(유한 상태 머신) 우선 적용 등 [`구현 패턴.md`](구현%20패턴.md) 참조

## 5. 단순 수정 예외

일반 규칙 §7과 동일. 오타·설정값·자명한 한 줄 수정은 4단계 면제. 단 코드 변경이 광범위하거나 회귀 위험이 있으면 적용.

## 6. 관련 지침

- [`../일반/작업 진행 규칙.md`](../일반/작업%20진행%20규칙.md) — 4단계 프로세스 본문 (상속 원본)
- [`../일반/문서 작성 규칙.md`](../일반/문서%20작성%20규칙.md) — 산출물 위치·파일명·frontmatter
- [`네이밍 컨벤션.md`](네이밍%20컨벤션.md) — `tdc_` 접두어 등
- [`구현 패턴.md`](구현%20패턴.md) — FSM 우선 등
- [`분석·디버깅 규칙.md`](분석·디버깅%20규칙.md) — 회귀 fix·디버깅 시 추가 규칙
