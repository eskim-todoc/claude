# Codex Project Context

이 파일은 Codex가 `E:\Claude` 루트에서 작업할 때 따르는 프로젝트 지침이다. 근거 파일은 루트 [`CLAUDE.md`](CLAUDE.md)이며, `CLAUDE.md`의 현재 저장 상태에는 문자 깨짐이 있어 확인 가능한 구조와 규칙만 반영한다.

## 시작 규칙

- 새 작업을 시작하면 먼저 루트 `CLAUDE.md`와 이 `AGENTS.md`를 확인한다.
- 가능한 한 한국어로 답한다.
- 모르는 내용은 모른다고 말하고, 추측은 추측이라고 분리한다.
- 자료나 판단 근거가 있으면 출처를 명시한다.
- 설명이 복잡하면 표, Mermaid, 목록 등 시각적인 구조를 사용한다.

## 루트 기준

모든 설정과 산출물은 가능하면 프로젝트 루트 `E:\Claude`를 기준으로 둔다.

| 대상 | 원칙 |
|---|---|
| 프로젝트 설정 | 루트 또는 해당 프로젝트 폴더에 둔다. |
| 임시 작업 | `scratch/`를 우선 사용한다. |
| 공용 자동화 | `tools/`에 둔다. |
| 장기 기억/학습 | git tracked 문서인 `docs/지침/<적합 파일>/§학습 노트` (maturity 라벨) 또는 `.claude/memory/`를 사용한다. (2026-05-18 Package C 이후 `docs/회고/`는 폐지·지침 흡수) |
| 사용자 홈 설정 | 가능하면 `C:\Users\...` 아래에 새 설정 파일을 저장하지 않는다. |

## 폴더 구조

```text
E:\Claude\
|-- CLAUDE.md              루트 Claude 지침
|-- AGENTS.md              루트 Codex 지침
|-- .claude/memory/        수동 관리 메모리
|-- credentials/           서비스별 토큰, gitignored
|-- projects/              제품별 독립 repo, gitignored
|-- docs/                  공용 문서
|-- tools/                 공용 스크립트와 자동화
|-- scratch/               일회성 실험, gitignored
`-- .gitignore
```

## 작업 전 참조

작업을 시작하기 전에 `docs/지침/작업 규칙.md`를 우선 확인한다. 세부 규칙은 `docs/지침/` 아래의 문서들을 따른다.

| 영역 | 참조 위치 |
|---|---|
| 전체 체크리스트 | `docs/지침/작업 규칙.md` |
| 일반 커뮤니케이션 | `docs/지침/일반/커뮤니케이션 규칙.md` |
| 명령 해석 | `docs/지침/일반/명령 해석 규칙.md` |
| 문서 작성 | `docs/지침/일반/문서 작성 규칙.md` |
| 컨텍스트 절약 | `docs/지침/일반/컨텍스트 절약 규칙.md` |
| Git | `docs/지침/Git/` |
| 코딩 | `docs/지침/코딩/` |

## 프로젝트 작업

- 제품별 프로젝트는 `projects/<이름>/` 아래의 독립 repo로 취급한다.
- 프로젝트 내부에 별도 `CLAUDE.md`나 `AGENTS.md`가 있으면 더 가까운 파일의 지침을 우선한다.
- 가치 있는 공용 결과물은 `scratch/`에 남기지 말고 적절한 `projects/`, `docs/`, `tools/`로 승격한다.

## 인증 정보

- 인증 정보는 `credentials/` 아래에서만 다룬다.
- 토큰 값은 출력하거나 문서에 복사하지 않는다.
- `credentials/`, `projects/`, `scratch/`는 루트 `CLAUDE.md` 기준으로 gitignored 영역이다.

## 메모리와 회고

- 사용자 홈의 글로벌 auto-memory나 `C:\Users\...` 아래 새 메모리 파일은 가능하면 사용하지 않는다.
- 반복 가능한 배움이나 사용자의 명시적 선호는 승인 후 `docs/지침/<적합 파일>/§학습 노트` 섹션에 `*(maturity: experimental)*` 라벨로 추가 → 검증 후 본문 통합·`stable`/`core`로 라벨 승급 (2026-05-18 Package C). 또는 루트의 추적 문서에 기록한다.
- `.claude/memory/`는 루트 프로젝트 안의 수동 관리 메모리로만 사용한다.

## 자체 검증

응답이나 변경 전후에 다음을 확인한다.

| 검증 항목 | 확인 방법 |
|---|---|
| 경로 기준 | `E:\Claude` 루트 기준인지 확인 |
| 기존 변경 보호 | `git status --short`로 사용자 변경을 확인 |
| 인코딩/문자 | 한글 파일은 깨짐 여부를 확인 |
| 출처 | 로컬 파일, 명령 출력, 공식 문서 등 근거를 명시 |
| 불확실성 | 복원 불가하거나 확인하지 못한 내용은 단정하지 않음 |

